---
title: Authentication model
description: Understand how access is enforced.
---

Valora separates UI authentication from API authentication.

## Control plane
Valora Cloud uses user sessions and role-based access control for configuration.

## Runtime
The runtime uses API keys scoped to workspaces and permissions for ingestion and queries.
